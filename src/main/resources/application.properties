spring.application.name=banking-system
spring.datasource.url=jdbc:mysql://localhost:3306/online_banking_system
spring.datasource.username=root
spring.datasource.password=Sidd@3221
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
logging.level.org.hibernate=DEBUG
server.port=8083

spring.thymeleaf.cache=false

# Set a custom session cookie name
# server.servlet.session.cookie.name=MYSESSIONID

# Make cookies secure (HTTPS required)
server.servlet.session.cookie.secure=true

# HttpOnly flag for cookies (prevents JavaScript access)
server.servlet.session.cookie.http-only=true

spring.jpa.open-in-view=false
